import { SupplementListProps } from './types';
import { SupplementList as WebComponent } from '../../../../apps/web/client/src/components/SupplementList';

// React Native implementation
export const SupplementList = (props: SupplementListProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
