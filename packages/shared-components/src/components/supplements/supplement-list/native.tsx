import { supplement-listProps } from './types';
import { supplement-list as WebComponent } from '../../../../apps/web/client/src/components/supplement-list';

// React Native implementation
export const supplement-list = (props: supplement-listProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
