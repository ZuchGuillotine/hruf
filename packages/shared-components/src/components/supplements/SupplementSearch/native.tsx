import { SupplementSearchProps } from './types';
import { SupplementSearch as WebComponent } from '../../../../apps/web/client/src/components/SupplementSearch';

// React Native implementation
export const SupplementSearch = (props: SupplementSearchProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
