import { SupplementFormProps } from './types';
import { SupplementForm as WebComponent } from '../../../../apps/web/client/src/components/SupplementForm';

// React Native implementation
export const SupplementForm = (props: SupplementFormProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
