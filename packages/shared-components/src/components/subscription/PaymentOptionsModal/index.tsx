import { PaymentOptionsModalProps, PaymentOptionsModalState } from './types';

// Platform-agnostic logic and types
export const PaymentOptionsModal = (props: PaymentOptionsModalProps) => {
  // Add shared logic here
  return null;
};
