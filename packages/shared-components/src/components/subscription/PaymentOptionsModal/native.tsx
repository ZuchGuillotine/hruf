import { PaymentOptionsModalProps } from './types';
import { PaymentOptionsModal as WebComponent } from '../../../../apps/web/client/src/components/PaymentOptionsModal';

// React Native implementation
export const PaymentOptionsModal = (props: PaymentOptionsModalProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
