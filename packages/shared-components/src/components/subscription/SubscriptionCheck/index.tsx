import { SubscriptionCheckProps, SubscriptionCheckState } from './types';

// Platform-agnostic logic and types
export const SubscriptionCheck = (props: SubscriptionCheckProps) => {
  // Add shared logic here
  return null;
};
