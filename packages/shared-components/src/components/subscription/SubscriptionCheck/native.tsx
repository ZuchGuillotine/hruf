import { SubscriptionCheckProps } from './types';
import { SubscriptionCheck as WebComponent } from '../../../../apps/web/client/src/components/SubscriptionCheck';

// React Native implementation
export const SubscriptionCheck = (props: SubscriptionCheckProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
