import { ValuePropositionProps } from './types';
import { ValueProposition as WebComponent } from '../../../../apps/web/client/src/components/ValueProposition';

// React Native implementation
export const ValueProposition = (props: ValuePropositionProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
