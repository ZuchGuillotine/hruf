import { LimitReachedNotificationProps, LimitReachedNotificationState } from './types';

// Platform-agnostic logic and types
export const LimitReachedNotification = (props: LimitReachedNotificationProps) => {
  // Add shared logic here
  return null;
};
