import { LimitReachedNotificationProps } from './types';
import { LimitReachedNotification as WebComponent } from '../../../../apps/web/client/src/components/LimitReachedNotification';

// React Native implementation
export const LimitReachedNotification = (props: LimitReachedNotificationProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
