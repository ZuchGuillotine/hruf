import { ProfileProgressProps } from './types';
import { ProfileProgress as WebComponent } from '../../../../apps/web/client/src/components/ProfileProgress';

// React Native implementation
export const ProfileProgress = (props: ProfileProgressProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
