import { CookieConsentProps } from './types';
import { CookieConsent as WebComponent } from '../../../../apps/web/client/src/components/CookieConsent';

// React Native implementation
export const CookieConsent = (props: CookieConsentProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
