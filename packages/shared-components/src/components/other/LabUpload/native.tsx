import { LabUploadProps } from './types';
import { LabUpload as WebComponent } from '../../../../apps/web/client/src/components/LabUpload';

// React Native implementation
export const LabUpload = (props: LabUploadProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
