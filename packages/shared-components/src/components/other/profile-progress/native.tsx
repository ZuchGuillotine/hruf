import { profile-progressProps } from './types';
import { profile-progress as WebComponent } from '../../../../apps/web/client/src/components/profile-progress';

// React Native implementation
export const profile-progress = (props: profile-progressProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
