import { SupplementStreakCardProps } from './types';
import { SupplementStreakCard as WebComponent } from '../../../../apps/web/client/src/components/SupplementStreakCard';

// React Native implementation
export const SupplementStreakCard = (props: SupplementStreakCardProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
