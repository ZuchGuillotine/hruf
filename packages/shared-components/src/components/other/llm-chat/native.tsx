import { llm-chatProps } from './types';
import { llm-chat as WebComponent } from '../../../../apps/web/client/src/components/llm-chat';

// React Native implementation
export const llm-chat = (props: llm-chatProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
