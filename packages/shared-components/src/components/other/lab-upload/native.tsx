import { lab-uploadProps } from './types';
import { lab-upload as WebComponent } from '../../../../apps/web/client/src/components/lab-upload';

// React Native implementation
export const lab-upload = (props: lab-uploadProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
