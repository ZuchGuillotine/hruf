import { SummaryTriggerProps } from './types';
import { SummaryTrigger as WebComponent } from '../../../../apps/web/client/src/components/SummaryTrigger';

// React Native implementation
export const SummaryTrigger = (props: SummaryTriggerProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
