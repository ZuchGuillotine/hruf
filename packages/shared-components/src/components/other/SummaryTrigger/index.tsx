import { SummaryTriggerProps, SummaryTriggerState } from './types';

// Platform-agnostic logic and types
export const SummaryTrigger = (props: SummaryTriggerProps) => {
  // Add shared logic here
  return null;
};
