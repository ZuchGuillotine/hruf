import { BackgroundWordsProps } from './types';
import { BackgroundWords as WebComponent } from '../../../../apps/web/client/src/components/BackgroundWords';

// React Native implementation
export const BackgroundWords = (props: BackgroundWordsProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
