import { background-wordsProps } from './types';
import { background-words as WebComponent } from '../../../../apps/web/client/src/components/background-words';

// React Native implementation
export const background-words = (props: background-wordsProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
