import { ProfileCompletionNotificationProps } from './types';
import { ProfileCompletionNotification as WebComponent } from '../../../../apps/web/client/src/components/ProfileCompletionNotification';

// React Native implementation
export const ProfileCompletionNotification = (props: ProfileCompletionNotificationProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
