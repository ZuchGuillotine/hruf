import { LandingHeaderProps } from './types';
import { LandingHeader as WebComponent } from '../../../../apps/web/client/src/components/LandingHeader';

// React Native implementation
export const LandingHeader = (props: LandingHeaderProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
