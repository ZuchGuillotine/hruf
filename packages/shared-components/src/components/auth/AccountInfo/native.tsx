import { AccountInfoProps } from './types';
import { AccountInfo as WebComponent } from '../../../../apps/web/client/src/components/AccountInfo';

// React Native implementation
export const AccountInfo = (props: AccountInfoProps) => {
  // TODO: Implement React Native version
  // For now, we can use the web component as a reference
  return null;
};
